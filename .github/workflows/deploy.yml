name: Deploy to EC2

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Java 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build with Maven
        run: mvn clean package -DskipTests

      - name: Copy JAR to EC2
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_PRIVATE_KEY }}
          source: "target/*.jar"
          target: "/opt/eatyaar/"
          strip_components: 1

      - name: Deploy on EC2
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_PRIVATE_KEY }}
          script: |
            cat > /opt/eatyaar/.env << EOF
            DB_USERNAME=${{ secrets.DB_USERNAME }}
            DB_PASSWORD=${{ secrets.DB_PASSWORD }}
            JWT_SECRET=${{ secrets.JWT_SECRET }}
            MAIL_PASSWORD=${{ secrets.MAIL_PASSWORD }}
            MAIL_TEST_RECIPIENT=${{ secrets.MAIL_TEST_RECIPIENT }}
            EOF

            # Remove old JARs first
            rm -f /opt/eatyaar/eatyaar.jar
            rm -f /opt/eatyaar/*.jar.old

            # Rename new JAR
            mv /opt/eatyaar/eatyaar-0.0.1-SNAPSHOT.jar /opt/eatyaar/eatyaar.jar

            sudo systemctl restart eatyaar
            echo "✅ Deployed successfully"